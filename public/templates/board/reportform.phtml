<form id="reportform" class="reportform" method="post">
  <p>Reporting post <strong><?=$post['post_id']?></strong> on board <strong>/<?=$post['board_id']?>/</strong>.</p>
  <fieldset id="reportform-type">
    <legend>Report type</legend>
    <?php
      $select_input = '<select id="type" name="type">';
      $select_options = '<option value=""></option>';
      foreach (MB_REPORT_TYPES as $key => $val) {
        $select_options .= "<option value='{$key}'>{$val}</option>";
      }
      echo $select_input . $select_options . '</select>';
    ?>
  </fieldset>
  <p>Submitting <strong>false</strong> or <strong>misclassified</strong> reports will result in a ban.</p>
  <?php
    if (MB_CAPTCHA_REPORT) {
      echo '<div class="h-captcha" data-sitekey="' . MB_CAPTCHA_HCAPTCHA_SITE . '"></div>';
    }
  ?>
  <button type="submit">Submit</button>
</form>
