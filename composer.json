{
    "require": {
        "php-ffmpeg/php-ffmpeg": "^1.3",
        "james-heinrich/getid3": "^1.9"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5"
    },
    "scripts": {
        "test": [
            "./vendor/bin/phpunit -c phpunit.xml --colors=always --stop-on-failure --coverage-text --coverage-clover coverage.xml --coverage-html ./coverage"
        ]
    }
}
