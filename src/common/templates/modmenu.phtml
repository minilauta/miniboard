<?php
  foreach ($app->get_modules() as $module) {
    $module_index = $module->get_index();
    if (empty($module_index)) continue;
    $module_name = ucfirst($module->get_name());
    echo "[<a href='{$module->get_index()}/'>{$module_name}</a>] ";
  }
?>
